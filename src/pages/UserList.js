import React from 'react';
import Header from '../components/Header';

const UserList = () => {
    return (
        <div>
            <Header />
            test
        </div>
    );
};

export default UserList;